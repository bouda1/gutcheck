# Runtime dependencies of gutcheck.
# Install : uv pip install -r requirements.txt
# Upgrade : uv pip install --upgrade -r requirements.txt

babel>=2.18.0   # format_date / parse_date / parse_time (gutcheck.py, simu.py)
numpy>=2.5.2    # simulation and linear algebra (model.py, simu.py)
scipy>=1.18.1   # scipy.optimize.nnls (model.py)
